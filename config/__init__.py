"""
This package centralizes the application's configuration.

It exposes the VLM (Vision-Language Model) client and the environment
variables for easy access throughout the application.
"""
from .env_config import env
from .vlm_config import VLM
